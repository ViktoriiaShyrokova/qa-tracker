<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!-- Формат: время уровень [класс] сообщение -->
            <pattern>%d{HH:mm:ss.SSS} %-5level [%logger{25}] %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Наш код: видим DEBUG и выше -->
    <logger name="org.qatracker" level="DEBUG"/>

    <!-- Всё остальное: только INFO и выше -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>